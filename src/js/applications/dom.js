// FPs || './$dir', 'c1', 'c2'
this.pageSrcs = ['./applications', 'p1', 'p2', 'p3']
/*
{
    "input": 
        "text" (*) || "select" (^) || "date" (%) || "number" (#) ||
        --* autocomplete
        --# other
        -- 
        ----* inline
        ----# 
        ----*# list

    inputOpts {
        *: 
        ^: 
        %: 
        #: 
    }
}
*/
const makeInput = opts => {

}